sudo: required
services:
    - docker

before_install:
    - docker build -t chendu36/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true chendu36/docker-react npm run test -- --coverage
